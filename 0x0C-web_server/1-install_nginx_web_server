#!/usr/bin/env bash
# Update package list and install Nginx webserver

# Install nginx
sudo apt-get update
sudo apt-get install nginx -y

# Configure nginx to serve a "Hello World!" page
sudo sed -i 's|location / {|location / { echo "Hello World!";|' /etc/nginx/sites-available/default

# Restart nginx
sudo service nginx restart
